name: Lottery API Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Game IDs
        run: |
          for id in {1..20}
          do
            echo ""
            echo "========== GAME ID $id =========="

            curl -s "https://wordpresswebapi.azurewebsites.net/api/ElectronicDrawing/ElectronicDrawingDateSearch/$id/2026-06-16/2026-06-19%2023:59" | head -c 500

            echo ""
            echo ""
          done
